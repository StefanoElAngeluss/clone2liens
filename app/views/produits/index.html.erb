<p style="color: green"><%= notice %></p>

<div class="d-flex">
    <h1>Produits</h1>
</div>

<div id="produits">
  <% @produits.each do |produit| %>
    <%= render produit %>
    <p>
      <%= link_to "Show this produit", produit %>
    </p>
  <% end %>
</div>
