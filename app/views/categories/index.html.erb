<p style="color: green"><%= notice %></p>

<h1>Categories</h1>

<div id="categories">
  <% @categories.each do |cat| %>
    <%= render cat %>
    <p>
      <%= link_to "Show this category", cat %>
    </p>
  <% end %>
</div>

<%= link_to "New category", new_category_path %>
