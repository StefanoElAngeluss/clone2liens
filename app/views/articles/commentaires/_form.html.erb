<%= form_with(model: [article, commentaire]) do |f| %>
    <div class="form-control">
        <%= f.rich_text_area :contenu, class: "form-control" %>
        <%= f.submit submit_label, class: "btn btn-primary mt-1" %>
    </div>
<% end %>